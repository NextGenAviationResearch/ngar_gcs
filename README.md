# Ground control station workspace for ardupilot vehicles
It is used to operate ArduPilot-based vehicles.

## Build
```bash
docker compose -f docker/Dockerfile build
```

## Run (Linux/X11)
```bash
docker compose -f docker/Dockerfile up
```

